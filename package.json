{
  "name": "playwright-cucumber-example",
  "version": "1.0.0",
  "scripts": {
    "test": "cucumber-js --require-module ts-node/register --require features/steps/**/*.ts features",
    "build": "tsc",
    "playwright:verify": "npx playwright test"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6",
    "playwright": "^1.32.0"
  }
}