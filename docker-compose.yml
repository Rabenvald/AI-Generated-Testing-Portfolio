version: '3.8'
services:
  test:
    build: .
    volumes:
      - ./:/usr/src/app
    working_dir: /usr/src/app
    command: ["npx", "cucumber-js", "--require-module", "ts-node/register", "--require", "features/steps/**/*.ts", "features"]